name: example
description: An example app that showcases filterable usage.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  bloc: ^8.1.2
  filterable:
    path: ../packages/filterable
  filterable_annotation:
    path: ../packages/filterable_annotation
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.3
  flutter_localizations:
    sdk: flutter
  freezed_annotation: ^2.4.1
  intl: ^0.19.0

dev_dependencies:
  bloc_test: ^9.1.2
  build_runner: ^2.4.6
  filterable_generator:
    path: ../packages/filterable_generator
  flutter_test:
    sdk: flutter
  freezed: ^2.4.1
  json_serializable: ^6.7.1    
  mocktail: ^0.3.0
  very_good_analysis: ^5.0.0

flutter:
  uses-material-design: true
  generate: true
